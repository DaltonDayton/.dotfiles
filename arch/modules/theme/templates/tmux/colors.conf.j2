# Theme: {{ meta.name }}
# Generated by theme switcher — do not edit directly.
# Edit the template at modules/theme/templates/tmux/colors.conf.j2
#
# Overrides the Catppuccin tmux plugin's @thm_* variables.

set -g @thm_bg "{{ base }}"
set -g @thm_blue "{{ blue }}"
set -g @thm_green "{{ green }}"
set -g @thm_maroon "{{ maroon }}"
set -g @thm_mauve "{{ mauve }}"
set -g @thm_overlay_0 "{{ overlay0 }}"
set -g @thm_peach "{{ peach }}"
set -g @thm_pink "{{ pink }}"
set -g @thm_red "{{ red }}"
set -g @thm_rosewater "{{ rosewater }}"
set -g @thm_surface_0 "{{ surface0 }}"
set -g @thm_yellow "{{ yellow }}"
